# Tablas del proyecto (FASE 1)

La guía completa está en la **raíz del repositorio** para que aparezca al buscar en Cursor:

**`TABLAS_PROYECTO.md`** (en la carpeta principal del proyecto, junto a `README.md`).

En **Ctrl+P** escribe `TABLAS` y ábrelo. Los modelos siguen en esta carpeta (`*.py`).
